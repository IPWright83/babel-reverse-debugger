const calculator = {
  add: (a, b) => { return a + b; }, 
  subtract: function(a, b) { return a - b; },
  multiply: (a, b) => a * b,
  divide: (a, b) => { return a / b; },
  clear: function() { return; },
  cancel: () => {},
};
